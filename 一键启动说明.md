# 一键启动说明（Windows）

首次使用先双击 `检查启动环境.bat`，完成后日常只需双击 `启动项目.bat`。两个入口均从脚本自身所在目录推导路径，不依赖固定盘符、Windows 用户名、Anaconda 路径或当前工作目录。

## 第一步：检查启动环境（首次或更新后运行）

1. 检查 Node.js `20.19+` 或 `22.12+`、npm、uv 和 Docker Desktop，并实际执行一段 JavaScript，避免“版本正确但 Node 本身不可运行”的假通过。
2. 缺少工具时，说明安装原因并询问是否通过 Windows 包管理器自动安装；系统需要时会弹出管理员授权。
3. 自动创建 `Code/.env` 和 `paper-claw-master/.env`。D 端的 JWT 密钥会随机生成，不会使用示例弱密钥。
4. 检查并补齐 Node/Python 依赖。`uv` 会按项目声明自动准备 Python 3.12+ 环境，无需固定安装 Anaconda。
5. 启动 Docker Desktop、PostgreSQL，等待数据库健康后执行迁移。

环境准备完成后会提示可以使用 `启动项目.bat`。

## 第二步：一键启动（日常使用）

1. 保持命令窗口可见；失败会显示原因并返回非零退出码，不再静默地显示一个无法打开的启动页。
2. 只使用已经准备好的依赖，但每次都会检查运行时数据、启动 Docker/PostgreSQL，并执行数据库迁移。首次数据库迁移可花约两分钟；详细输出会写入 `.startup-logs/migration.log`，完成后显示在窗口。
3. 拉起 A 后端（默认 8000）、D 后端（默认 3001）和前端（默认 5173）。服务已占用端口时，会核验它确实是本平台服务；不匹配则明确报冲突。
4. 只有 A 健康接口、D 健康接口且 RAG 已加载并报告导师数量、以及前端 HTML 都通过后，才自动打开浏览器。云图接口本身需要用户登录，不会被启动器以匿名请求误判为故障。
5. 日志保存在 `.startup-logs/a-backend.log`、`.startup-logs/d-backend.log` 和 `.startup-logs/frontend.log`。

## 端口与多副本运行

- D 后端端口由 `Code/.env` 的 `PORT` 控制；前端端口由 `VITE_PORT` 控制。前端代理默认自动使用同一个 `PORT`，如需访问远程 D 后端才设置 `VITE_API_PROXY_TARGET`。
- A 服务地址由 `MENTOR_AGENT_BASE_URL` 控制；启动器会从该 URL 读取端口。
- PostgreSQL 宿主端口由 `paper-claw-master/.env` 的 `PAPER_CLAW_POSTGRES_PORT` 控制。若改动它，必须同时把 `PAPER_CLAW_DATABASE_URL` 内的端口改为相同值；启动器会提前拦截不一致配置。
- Docker Compose 使用由项目绝对路径派生的项目名，不再占用全局固定容器名。不同目录的多个副本可并存；但若它们使用同一数据库端口，仍应为其中一个副本改端口。

## 新电脑首次运行可能需要人工确认的事项

- **Docker Desktop**：首次安装可能要求注销/重启、启用 WSL 2，并接受 Docker 协议。完成后重新双击启动即可。
- **模型/API 功能**：导师本地 RAG 无需聊天模型密钥；聊天、报告等模型功能需要在 `paper-claw-master/.env` 填写 `PAPER_CLAW_CHAT_API_KEY`、`PAPER_CLAW_CHAT_BASE_URL` 和 `PAPER_CLAW_CHAT_MODEL`。PDF 分析在未配置聊天模型时会自动使用本地多语种语义排序，不会因为缺少模型配置而上传失败；配置模型后会自动启用模型重排。
- **邮件功能**：发送/收取邮件需要在 `Code/.env` 填写 SMTP/IMAP 参数。没有这些参数不影响网站和导师检索启动。
- **校园网/IPv6**：若实时访问中科大官网、arXiv 或 OpenAlex 超时，启动器不会擅自修改系统网络设置；本地 RAG 仍可使用。请切换正常网络/VPN，或由电脑管理员处理 IPv6 路由。

## 命令行用法

完整准备环境：

```powershell
.\检查启动环境.bat
```

日常启动：

```powershell
.\启动项目.bat
.\启动项目.bat -NoBrowser     # 启动成功后不自动打开浏览器
```

## 常见失败位置

- `npm install failed`：检查 npm 网络代理、磁盘空间，再重试。
- `uv sync failed`：检查 Python 包下载网络；uv 会自动管理 Python，不需要手工建立 `.venv`。
- `Docker did not become ready`：打开 Docker Desktop 完成 WSL 2/协议初始化，再重新启动。
- `PostgreSQL did not become healthy`：查看 Docker Desktop 和 `.startup-logs/`。
- 迁移步骤超过两分钟或报错：查看 `.startup-logs/migration.log`；先运行一次 `检查启动环境.bat`，确保 Python 依赖已完整准备。
- `Node.js is present but cannot execute JavaScript`：这是 Node 运行时异常，不是项目依赖缺失；修复或重装 Node.js LTS 后重试。
- A/D/前端启动失败：分别查看 `.startup-logs/a-backend.log`、`.startup-logs/d-backend.log`、`.startup-logs/frontend.log`。
- `Business smoke check failed`：服务端口虽已启动，但 D 未能返回星图导师数据；检查 D 日志、RAG 数据文件和数据库迁移。

## 交付前的必做检查

在发布 ZIP 或让新电脑克隆前，运行：

```powershell
node .\scripts\verify-runtime-data.mjs .\paper-claw-master\data\ustc_mentor_rag.json .\cloud3d\cloud_data.json
```

它会拒绝损坏 JSON 或两套数据的导师 ID 不一致，并在星图派生数据的时间/证据数落后 RAG 时给出警告。出现警告时先在可用 Python 环境重建 `cloud3d/cloud_data.json`，再交付。

此外，发布分支必须包含本启动方案和所有运行时数据文件；仓库默认分支若仍指向旧版启动脚本，新电脑普通 `git clone` 不会获得这些修复。
