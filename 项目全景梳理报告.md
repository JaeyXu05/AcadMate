# 项目全景梳理报告

> 本文档是**独立整理的代码级全景结论文档**，供单独打开查阅。
> 内容来自对仓库全部关键代码的通读（含对实际 JSON 数据的计数核对），标注了"文档与代码不一致"之处。
>
> - 目的：给阅读者（尤其是接手的新人 / 后续 agent）一份准确、结构清晰的总体认知。
> - 定位：**导读式汇总**，不替代各模块文档，只把全貌讲清楚。
> - 数据基准：2026-09-02；RAG 库以磁盘上 `data/ustc_mentor_rag.json` 为准。

---

## 0. 一句话概述

学生登录网站 → 输入研究兴趣 → A 多智能体从当前 972 位中科大导师中匹配打分 → 展示卡片 / 详情 / 收藏 / 邮件；星图另用 715 节点历史快照。

---

## 1. 四部分分工

| 角色 | 责任 | 目录 |
|---|---|---|
| **D** | 网站全栈（React+Express+SQLite） | `Code/` |
| **A** | 多智能体检索后端 | `paper-claw-master/backend` |
| **C** | 抓取官网 + 数据库（RAG） | `paper-claw-master/data_scripts` + `data` |
| **B** | 3D 研究星图（数据生成） | `cloud3d/` |

> 注：`cloud3d/` 现在只是**数据生成**模块（`build_cloud.py` → `cloud_data.json`）；真正的 3D 渲染前端是 D 的 `Code/src/components/CloudGraph.tsx`。详见第 7 节。

---

## 2. 核心主线：candidate_id 与 RAG 库

- **`candidate_id`**（形如 `ustc_faculty_26275`）是每位导师的唯一 ID，四部分共享。来源：C 的 `build_rag.py` 用 `_candidate_id(faculty_id) = f"ustc_faculty_{faculty_id}"` 生成；A 的 `ustc_sources.py::mentor_candidate_id(name, faculty_id=...)` 在有 faculty_id 时也产出同一形态。A 检索结果 `id` ⇔ C 知识库导师 `id` ⇔ `GET /api/advisors/:id` ⇔ 云图 `CloudNode.id` 必须一致。
- **当前 RAG 库** `paper-claw-master/data/ustc_mentor_rag.json`：**972 位导师 / 1969 条证据**。`cloud3d/cloud_data.json` 的 **715 节点**是独立历史快照，不是当前检索库人数。
- 字段（candidate）：`candidate_id / mentor_name / affiliation / department / research_topics / methods / publications / projects / homepage / recruitment_status / evidence_refs / missing_fields / source_metadata{ustc_faculty_id, english_name, academic_title, mentor_role, paper_platforms, topics_source}`。
- 字段（evidence）：`evidence_id / candidate_id / source_type / source_uri / title / extracted_fact / locator / retrieved_at / freshness / confidence / content_hash / metadata{identity_verified, mentor_role_verified, supports_fields, ...}`。
- 字段全部与后端 schema（`backend/mentor_workflow/schemas.py` 的 `CandidateMentor` / `EvidenceRecord`）对齐。

### RAG 库真实计数（核对磁盘 JSON，非文档）

| 项 | 值 |
|---|---|
| `mentor_count` / `len(candidates)` | **972**（721 结构字段核验 + 251 官方主页强身份语句或人工复核；2 位证据不足未进仓） |
| `evidence_count` / `len(evidence)` | **1969** |
| 有 `research_topics` | **668 / 972**（597 来自官网 profile + 71 由论文标题回填） |
| 有确认 `publications` | 259 / 972 |
| 有 `methods` | 45 / 972 |
| 有 `recruitment_status` | 107 / 972 |
| 有清洗后的 `profile_bio` | 758 / 972 |
| 有 `email` | 193 / 972 |
| 有 `homepage` / `department` / `affiliation` | 972 / 972 / 972（100%） |
| `source_chain` | `["internal_ustc_rag"]` |
| `topics_source` 分布 | 0 无方向 304 / 1 来自 profile 597 / 2 论文回填 71 |
| 证据按 `source_type` | 身份 972 + profile 630 + openalex 200 + s2 150 + dblp 17 = 1969 |
| 论文证据状态 | 367 条 confirmed / 0 条 pending_review；191 个歧义平台命中已人工裁决（41 保留 / 150 排除） |
| `candidate_id` 形态 | 全部匹配 `^ustc_faculty_\d+$` |

---

## 3. 【D】Code/ —— 网站全栈

### 3.1 技术栈

| 层 | 技术 | 白话 |
|---|---|---|
| 前端 | React 18.3 + TS 5.6 + AntD 5.22 + Zustand 5 + Vite 5 + Three.js 0.185 | 组件化网页 + 类型检查 + UI + 全局状态 + 3D |
| 后端 | Express 4.22 + TS + tsx 运行 | Node Web 服务 |
| 数据库 | better-sqlite3 `^13.0.3` | 文件型数据库 `Code/data.db`（含 WAL） |
| 认证 | JWT（7 天有效）+ bcryptjs（cost 10） | 令牌 + 密码加密 |

> `package.json` **无 `engines` 字段**；脚本 5 个：`dev`（concurrently 跑 vite + tsx server/index.ts）、`dev:frontend`、`dev:backend`、`build`（tsc -b + vite build）、`preview`。端口：前端 5173、后端 3001。

### 3.2 后端数据库（5 张表 + 2 索引）

`server/db.ts` 用 `PRAGMA user_version` 做版本迁移（`SCHEMA_VERSION=1`）；开库即设 `journal_mode=WAL`、`foreign_keys=ON`；DB 文件 `Code/data.db`。

| 表 | 用途 | 约束 |
|---|---|---|
| `users` | 用户 | `email UNIQUE`、`password_hash`（bcrypt）、`interests/skills` 存 JSON 字符串数组 |
| `favorites` | 收藏 | `UNIQUE(user_id, advisor_id)`、`FK(user_id→users) ON DELETE CASCADE` |
| `user_settings` | 设置 | `user_id UNIQUE NOT NULL`、`FK CASCADE`；4 字段 `bg_theme/bg_color/default_sort/card_density` |
| `search_history` | 检索历史 | `FK CASCADE`、索引 `idx_search_history_user(user_id, created_at DESC)` |
| `chat_history` | 对话历史 | `FK CASCADE`、索引 `idx_chat_history_session(user_id, session_id, created_at)` |

> 全库**无任何 `hIndex` 列**。

### 3.3 关键 API（`server/routes/*`，除 `/auth/login` 外均需 `Authorization: Bearer <token>`）

- `POST /api/auth/login` 登录即注册（邮箱正则 + 密码 ≥6 位）；路由级限流 20/5min/IP；返回 `{token, user}`。
- `POST /api/agent/chat`（**SSE 流式**，限流 12/min/IP）：D 作为代理转发给 A。
  - SSE 事件序列（每条 `event:<t>\ndata:<JSON>\n\n`，payload 都带 `type`）：`thinking`（可多次）→ `result`（`{advisors:Advisor[]}`，只发一次）→ `summary` → `done`（或失败时 `error`）。
  - 内部：`POST {A}/api/mentor-workflows`（`{message, execute_immediately:false}`）取 `trace_id` → 触发 `POST .../resume` → 每 `AGENT_POLL`（默认 1200ms）轮询 `GET .../events`（按 `message_id` 去重，命中 `EVENT_HINT` 才发 thinking）+ `GET .../status`（`COMPLETED`/`FAILED` 跳出）→ `GET .../result`。超时 `MENTOR_AGENT_TIMEOUT_MS`（默认 180000ms）。`MENTOR_AGENT_BASE_URL` 未配或 A 不可达 → 回退内联 mock（CV/AI/DEFAULT 三组假数据，id 1~8，**mock 带 hIndex**）。
  - **`mapFinalMentor()` 字段映射**（D 侧缝合关键）：`candidate.candidate_id→id`、`mentor_name→name`、`source_metadata.academic_title→title`、`department→department`、`research_topics→tags`、`publications.length→papers`、`match.total_score→matchScore`（四舍五入）、`match.rationale`(数组)→`explanation`（换行拼接）、`hIndex→undefined`。结果过滤掉 `name==='未知导师'`。
- `GET /api/advisors/:id` + `GET /api/advisors/:id/explanation`：**真实 RAG**（`server/data/ragAdvisors.ts` 读 RAG JSON 建 `byId`）。RAG 不可用 503，id 不存在 404。详情 `matchScore:0`（详情页无动态分）、`papers=publications.length`、`recentPapers=publications.slice(0,20).map(title)`（无 year/venue）。
- `POST /api/email/generate`：**真实 RAG + 确定性中文模板信**（无 AI），返回 `{subject, body}`。
- `POST /api/upload/pdf`（multer，魔数 `%PDF-` 校验，≤20MB，30min TTL）+ `POST /api/pdf/analyze`：**真实**——`unpdf` 抽正文 → `summary`/`keyPoints` 由内容生成（`server/routes/pdfText.ts`），`suggestedAdvisors` 用「文档关键词 ↔ RAG research_topics/publications」命中打分（无命中回退论文数前 3）。
- `GET /api/recommend`：**真实 RAG**，确定性关键词命中打分（`78+hits*6` 封顶 95；无命中 `55~72` 按 log2(papers)），取前 6，返回 `{recommendations, basedOn}`。
- `GET /api/cloud/graph`：读 `cloud3d/cloud_data.json` → 映射 `CloudNode`（`papers=pub_count`）→ 动态生成同域边（`buildEdges`：按 `domain` 分组，每人连**同域内最近邻 1 条**，`MAX_PER_DOMAIN=400`，`relation:"same-field"`，`weight=max(0.1,1-dist/400)`）。返回 `{nodes, edges, meta:{title, mentor_count, domain_count, legend, camera}}`。
- 收藏/历史/设置/用户：`/api/favorites`、`/api/history`、`/api/settings`、`/api/user`（profile GET/PUT、account DELETE，级联删）。

### 3.4 数据源真相（重要：纠正旧文档把多处标成 [STUB]）

- **`server/stub/advisors.ts`（8 条 mock，带 hIndex）已废弃**——无任何活动路由引用它。`agent.ts` 的回退 mock 是内联另写的。
- **`server/data/ragAdvisors.ts` 才是活数据源**：读 RAG JSON，被 `advisors/email/pdf/recommend` 及健康检查使用。
- 因此 **导师详情 / 邮件 / 推荐 / 健康检查 / PDF（summary+keyPoints+推荐）均为真实数据，不是 stub**；检索在 A 不可达时才回退 mock。

### 3.5 前端结构与展示字段

- 路由（`App.tsx`，`AuthGuard`/`GuestGuard` 守卫）：`/welcome`（落地，GuestGuard）→ 登录后 `/search`（首页）、`/cloud`、`/other`、`/profile`、`/settings`、`/favorites`、`/advisor/:id`、`/email`、`/compare`、`/pdf`、`/recommend`。
- 状态：`authStore`（token 存 localStorage/sessionStorage，7 天）、`searchStore`（聊天历史/结果/排序/分屏比 0.45，localStorage 持久化分屏）、`settingsStore`（4 主题预设 + 自定义色 + 排序 + 密度 + 语言，同步到后端）。
- **界面全用 `papers`（论文数）+ `matchScore`（匹配度）展示，全程无 hIndex**：`AdvisorCard`、`AdvisorDetailPage`、`ComparePage` 显示论文数+匹配度；`SortSelector` 排序项 = `匹配度/工号/论文数/院系`（无 hIndex 排序）。`Advisor.hIndex?` 在 `types/search.ts` 留可选字段备用，但 `mapFinalMentor`/`toAdvisorDetail`/`toLightAdvisor` 均不赋值。
- `CloudGraph.tsx`（Three.js，591 行）：`InstancedMesh`+自定义 `ShaderMaterial`（billboard+呼吸脉动），背景装饰用 `SPIRAL={r_in:170,r_out:540,turns:1.4,arms:6}` 画旋臂星尘/星云/中央亮核（**此 `arms:6` 仅为背景装饰，与 B 的 `build_cloud.py` 节点布局 `N_ARMS=4` 无关**）。节点坐标用 `cloud_data.json` 预计算值，组件不复算。交互：拖拽/缩放/自动缓旋、悬停浮卡、点击选中（按边高亮邻居）。
- `WelcomePage` 写死的 stats 文案原为 `1523`，已更正为 `1580 结构化证据`。

### 3.6 .env 变量

`JWT_SECRET`（≥16 字符且非默认，启动校验）、`PORT`(3001)、`CORS_ORIGINS`(默认 `http://localhost:5173`)、`MENTOR_AGENT_BASE_URL`(指向 A 的 :8000，空/不可达回退 stub)、`MENTOR_AGENT_TIMEOUT_MS`(180000)、`MENTOR_AGENT_POLL_MS`(1200)。

---

## 4. 【A】paper-claw-master/backend —— 多智能体检索后端

### 4.1 技术栈与配置

- FastAPI + SQLAlchemy 2 + PostgreSQL(pgvector) + Pydantic 2 + alembic；`requires-python >=3.12`；src 布局（import `backend.*`）。依赖含 `deepagents`、`langchain-openai/deepseek`、`openai`、`pgvector`、`pymupdf`、`tiktoken`。
- 状态：**默认确定性离线模式**——开关是 settings 字段 `mentor_workflow_model_reasoning_enabled`，**环境变量 `PAPER_CLAW_MENTOR_WORKFLOW_MODEL_REASONING_ENABLED`**（默认 False）。**注意：旧文档里的 `MODEL_REASONING_ENABLED` 是简写/不准确**。模型模式才需填 `PAPER_CLAW_CHAT_*` 模型 key。
- DB：`docker compose up -d` 起 pgvector/pg16；`alembic upgrade head` 建表。单条迁移 `0001_current_schema.py` 一次建好全部 ~29 张表。

### 4.2 工作流与状态

- `mentor_workflow/orchestrator.py` 的 `current_stage` 取自 **`WorkflowStage` 枚举的 9 个值**：`input_understanding → planning → domain_expert → mentor_research → matching → evidence_review → result_composer → completed`（或 `failed`）。
  - **没有独立的 `candidate_screening` / `paper_evidence_assessment` 阶段**——那两步在模型模式下是 `mentor_research` 阶段**内部**的子步骤（MENTOR_WORKFLOW.md 的"标准路径"图是逻辑描述，不是字面 `current_stage` 取值）。
- 编排：`input_understanding`（意图+缺失字段判定）→ 若需澄清则 `CLARIFICATION_REQUIRED` 直接返回（不跑研究/匹配/编排）；否则 `planning`（按 goal 生成 enabled 步骤）→ `_run_review_loop`（`domain_expert → mentor_research → matching → evidence_review`，审核不过由 `RetryController` 决定返工目标并循环，带阶段/总上限检查）→ `result_composer` → `completed`。
- 数据流：`MentorWorkflowRequest` → `IntentPacket` → `TaskPlan` → `candidates`(`MentorResearchResult`) → `match_results`(`MatchResult[]`) → `ReviewDecision` → `FinalResult{mentors:[{candidate,match}]}`。
- **目标(goal)跳过规则**（`agents/intake.py::_enabled_agents`）：`find_mentors`=全流程；`inspect_mentor`=跳过 domain_expert 与 matching；`compare_mentors`=跳过 domain_expert；`generate_contact_email`/`follow_up_question`=只跑 result_composer（复用已通过审核的状态）。

### 4.3 证据纪律与审核

- 证据必须带 `metadata.identity_verified=true`、`mentor_role_verified=true`、`supports_fields`，否则独立评审返回 `RESEARCH_AGAIN`。
- `EvidenceReviewAgent` 顺序检查：意图完整性 → 候选存在 → 证据引用闭环 → 字段是否被证据支持 → 候选有无研究方向 → 证据时效（`stale` 即 >5 年）→ 匹配一致性（仅 find/compare）。任一不过即返回对应决策。
- **`ReviewStatus` 枚举 5 值**：`PASS / REVISE / RESEARCH_AGAIN / NEED_MORE_INPUT / FAILED`。
- `EvidenceLedger` 按 `(source_uri, candidate_id, content_hash)` 去重；`content_hash = sha256(source_type+source_uri+candidate_id+extracted_fact+locator)`。

### 4.4 状态持久化（复用旧表，无新表）

- **复用 `AgentRun` / `AgentRunEvent`，不加任何 mentor 专属表**：完整 `WorkflowState` 序列化进 `AgentRun.output_json`；`request` 进 `input_json`；`trace_id` 存 `deepagent_run_id`；判别用 `workflow="mentor_search"`；事件进 `AgentRunEvent.payload_json`；`metadata_json` 标 `{"mentor_workflow":true, ...}`。
- 乐观锁：`state_version`（JSON 内字段）+ `with_for_update()` 行锁 + 创建时 `pg_advisory_xact_lock(hashtextextended(trace_id,0))`。

### 4.5 模型层与评分

- **Provider 抽象仅 OpenAI 兼容**（`integrations/llm/openai_compatible.py` 用 `openai` SDK 的 `chat.completions.create`；base_url/model/api_key 可配）。无原生 Anthropic/Google 适配器。模型模式需 `PAPER_CLAW_CHAT_MODEL`。
- **`MatchDimensionScores` 8 维**（全库 schema 无 hIndex）：`research_topic_match / method_match / application_match / recent_activity / student_background_fit / constraint_satisfaction / recruitment_fit / evidence_completeness`。`total_score = mean_score()`（0~100）。
- **`MatchResult`**：`candidate_id, total_score, dimension_scores, rationale:list[str], negative_factors, risks, uncertainty, evidence_refs, ranking_position`。
- 数据源链：内部 RAG（`get_internal_mentor_rag` 注入 `data_scripts.internal_mentor_rag.FileInternalMentorRag`，离线 TF-IDF：`score = cosine*100 + hits*3`；失败回退 `NullInternalMentorRag`）→ USTC 官方源（`*.ustc.edu.cn`，强制 HTTPS、重定向主机复检、响应类型/≤2MB 大小校验）→ 论文补全（**5 年时效规则**：current≤2yr、recent≤5yr、stale>5yr 丢弃；**作者归属校验**：论文绝不用于证明导师身份）。
- 两种 enricher：确定性 `MissingDirectionPaperEnricher`（只补缺方向候选）/ 模型 `AgenticPaperResearchEnricher`（语义初筛入围者逐人查论文，`min_relevance_score=35` 阈值）。

### 4.6 API（12 个端点，均 `/api/mentor-workflows`）

`POST` 创建（`execute_immediately=false` 可只建不跑）｜`GET /{trace_id}` 全状态｜`/status` 轮询状态｜`/events` 结构化事件｜`/candidates`｜`/matches`｜`/evidence`｜`/audit`（模型模式安全/工具轨迹）｜`/review` 评审决定｜`/result` 最终结果｜`POST /{trace_id}/input` 补充输入｜`POST /{trace_id}/resume` 继续。

> 旧 Paper-Claw 链路（`agents/`/`services/`/`tools/` 的 DeepAgents 对话/论文 QA/报告）**未被 mentor_workflow 改动**；mentor_workflow 仅复用其 AgentRun 表、PaperSearchService 与 chat provider，并新注册一个路由。

---

## 5. 【C】data_scripts + data —— 抓取 + RAG 组装

- 4 个自包含抓取脚本（仅依赖 `httpx`，不 import 后端）：
  - `ustc_scraper.py`：中科大官网教师库（9 个重点学院 ID + `--all-colleges` 全校模式 `USTC_ALL_TEACHING_UNITS_ID=""`），断点续抓，研究方向截断 20 条，输出 `data/ustc_mentors_raw.json`。
  - `openalex_scraper.py`：USTC 机构 ID `I126520041`，按被引取代表论文 → `ustc_mentor_papers.json`。
  - `semantic_scholar_scraper.py`：匿名 ~1 req/s（默认 `--delay 1.1`），按被引取 → `ustc_mentor_papers_s2.json`。
  - `dblp_scraper.py`：XML 端点 `/pid/{pid}.xml`（兼容数字 pid），按年份取 → `ustc_mentor_papers_dblp.json`。
- `build_rag.py`：清洗官网页面导航/计数/栏目模板，聚合 OpenAlex + S2 + DBLP；仅人工裁决或规则确认的作者论文进入候选字段，未裁决命中使正式验证失败；按 DOI/平台 ID/规范化标题跨平台去重；分别保存论文总量与代表作数；使用强语境从官方主页恢复导师身份；`candidate_id = ustc_faculty_{faculty_id}`。
- `internal_mentor_rag.py`：`FileInternalMentorRag` 实现 `InternalMentorRag.retrieve()`，离线 TF-IDF（CJK 整词+bigram、峰值归一 TF、`idf=log(N/(1+df))`、`score=cosine*100+hits*3`）。
- `verify_rag.py`：**A–G 七项检查**（schema、引用闭环、身份跳源条件、召回、覆盖率、检索质量、论文/简介语义元数据）；D/F 在后端依赖缺失时使用标准库回退，不再跳过核心门禁。
- `audit_rag.py`：输出主题、简介、身份排除项的结构化审计，区分确定垃圾与人工复核项。
- `review_mentor_roles.py`：离线复核官方主页，必要时可联网刷新；结果写入角色 override，不能仅凭职称自动进仓。
- `test_data_quality.py`：覆盖科研“导航”保留、页面噪声清除、宽泛关键词不回填、论文去重及身份强语境。
- `export_fuzzy_review.py`：导出三平台模糊/非精确命中对照表（`fuzzy_review.md`/`.json`）供人工裁决；裁决写 `manual_overrides.json`，各 scraper 重跑时优先读。
- 字段对齐：`candidate_id / mentor_name / source_metadata.academic_title / research_topics` 等与后端 schema 一致。

### C 模块文档坑

- 两个 run2 日志（`build_rag_run2.log`/`verify_rag_run2.log`）**已过期**（mtime 2026-07-28，描述的是 1300 证据的更早构建），不代表当前库。
- `.bak` 文件与 715/1580 口径属于旧版本；当前检索 JSON 为 972/1969。云图仍有意保留 715 节点历史快照。
- S2/DBLP 先产出独立 JSON，再由 `build_rag.py` 统一做作者状态裁决并入三平台。

---

## 6. 【B】cloud3d/ —— 3D 星图数据生成

### 6.1 现状（重要：HANDOVER.md 已过时）

`cloud3d/` **仅 3 个实际文件**：`HANDOVER.md`、`build_cloud.py`、`cloud_data.json`（+ `__pycache__`）。HANDOVER.md 描述的 `index.html / server.js / start_starmap.bat / OPEN_STARMAP.bat / .previews/` 演示前端**已全部删除**——真正的星图前端是 D 的 `Code/src/components/CloudGraph.tsx`。

### 6.2 `build_cloud.py`（当前源码 = 真相）

- 把每位导师按 `department + research_topics + methods` 关键词映射到 **10 个研究领域**（`DOMAINS`，id 如 `physics_quantum / cs_ai / ...`，各带颜色与 keys）。
- **多臂对数螺旋银河盘布局**：**`N_ARMS = 4`**（4 条主旋臂，非 HANDOVER 说的 6）；`R_IN=170 / R_OUT=540`，`TURNS=1.4`。
- **10 领域按人口降序 rank=0..9，分到 4 条臂（`ARM_BY_RANK` = 3+3+2+2）**；目标半径 `R_CORE_EDGE=250 → R_RIM_EDGE=460`（`target_radius(rank)=250 + rank/9*(460-250)`，**非旧文档的 205→470 / 均匀角 `rank*(360/10)` / `best_arm_t()`**——那些是旧 6 臂版本的描述，当前代码已无此函数）。
- `thickness(jt)=26→13`；`core_lum=(1-r_norm)^1.6`（中心 1.0 → 外缘 0）；`lum=0.55+0.45*min(1,score)`（∈[0.55,1.0]）；`size∈[1.0,3.5]`；`random.seed(42)`。
- 输入 `../paper-claw-master/data/ustc_mentor_rag.json`，输出 `cloud_data.json`，结构 `{meta, nodes}`（**无 edges**）：
  - `meta`：`title / generated_at / source_chain / mentor_count / evidence_count / domain_count / departments / legend[10] / camera{target,radius:1500,r_out,r_in,arms:4} / layout{galaxy_spacing:540,cloud_scale:70,disk_radius:540,arms:4}`。
  - `nodes[]`（20 字段）：`candidate_id,name,department,affiliation,domain,domain_name,secondary,color,lum,core_lum,size,x,y,z,topics,methods,pub_count,pubs,homepage,recruitment`。

### 6.3 ⚠️ 磁盘上的 `cloud_data.json` 是旧文件，需重生成

磁盘 `cloud_data.json`（mtime 20:17，比当前 `build_cloud.py` 早 19 分钟）由**旧版生成器**产出，结构与当前代码**不一致**：
- 旧文件是 **26-galaxy 布局**（`galaxies[]`、每 node 带 `galaxy`/`galaxy_name`、689 条 `intra-galaxy` 边、`camera.arms=0`、**无 `legend` 键**）。
- D 的 `cloud.ts` 读 `data.meta.legend`——旧文件没有 → `/api/cloud/graph` 会返回 `legend:[]`、`domain_count:0`。

> **结论：需重跑 `cd cloud3d && py build_cloud.py` 重生成 `cloud_data.json`**，D 的云图才能正常显示领域图例。同域星座边由 D 的 `cloud.ts` 在请求时动态生成（1-最近邻，非 k 近邻），不在 cloud_data.json 里。

---

## 7. 四部分数据流

```
[官网/论文平台] --C抓取--> data/ustc_mentor_rag.json (972导师/1969证据)
                                    │
        ┌───────────────────────────┼────────────────────────────┐
        ▼                           ▼                            ▼
   [A] mentor_workflow          [B] build_cloud.py           [D] 后端 ragAdvisors.ts
   InternalMentorRag.retrieve()  → cloud_data.json(需重生成)   读 RAG → /api/advisors/:id 等
   → 匹配/评分/邮件/PDF           → D /api/cloud/graph          → 真实详情/邮件/推荐
        │                           → CloudGraph.tsx(Three.js)     检索经 D 代理接 A
        └── D /api/agent/chat (SSE) 轮询 A /api/mentor-workflows ──┘
```

四部分通过 **`candidate_id`** 关联；A 检索 id ⇔ C 知识库 id ⇔ D `/api/advisors/:id` ⇔ B `CloudNode.id`。

---

## 8. 文档与代码不一致（务必留意的坑）

> 本表记录「历史说明文档曾与代码/数据不符」之处；其中标记 ✅ 的第 3/4/5/9 项已在
> 2026-08-22 修正文档（`cloud3d/HANDOVER.md` 重写、旧计数更正、PDF 真实化），此处仅留作历史对照。

| # | 旧文档内容 | 代码/数据实际 | 位置 |
|---|---|---|---|
| 1 | 证据数 1523/1580、导师数 715/721 | 当前检索库为 **972 / 1969**；云图 715 是独立历史快照 | 根文档、C/D 数据说明 |
| 2 | RAG 数来自 run2 日志（1300 证据） | run2 日志已过期，描述更早构建 | `data/*_run2.log` |
| 3 | 悬臂数 6、`best_arm_t()`、均匀角 `rank*(360/10)`、半径 205→470 | 当前 `build_cloud.py`：**N_ARMS=4**、无 `best_arm_t`、半径 250→460、3+3+2+2 分臂 ✅HANDOVER 已重写 | `cloud3d/build_cloud.py`、HANDOVER |
| 4 | `cloud3d/` 含 `index.html/server.js/.bat/.previews` | **目录仅 4 文件**，演示前端已删；真前端在 D 的 `CloudGraph.tsx` ✅HANDOVER 已重写 | `cloud3d/` |
| 5 | 磁盘 `cloud_data.json` 有 `legend`/`arms=6`/无 edges | 旧文件无 `legend`；**已重生成**（`legend=10`/`domain_count=10`/`arms=4`）✅已重生成 | `cloud3d/cloud_data.json` |
| 6 | 模型开关 `MODEL_REASONING_ENABLED` | 实为 `PAPER_CLAW_MENTOR_WORKFLOW_MODEL_REASONING_ENABLED` | CLAUDE.md、README |
| 7 | 工作流阶段含独立 `Candidate Screening`/`Paper Evidence Assessment` | `WorkflowStage` 仅 9 值，那两步是 `mentor_research` 内部子步骤 | MENTOR_WORKFLOW.md |
| 8 | verify_rag "4/5 项检查" | 当前为 **A–G 七项**，含检索质量与语义元数据硬门禁 | data_scripts/README.md |
| 9 | 导师详情/邮件/推荐是 [STUB]、用 `stub/advisors.ts` | **均为真实 RAG**（`ragAdvisors.ts`）；`stub/advisors.ts` 已废弃；PDF summary/keyPoints 已真实化 ✅ | README §4.3 |
| 10 | S2/DBLP 曾写“暂不并入 RAG” | 当前由 `build_rag.py` 统一裁决后并入三平台 | 各 scraper docstring |
| 11 | D 的 `CloudGraph` 用 `arms:6` | `SPIRAL.arms:6` 仅背景装饰；节点布局来自 `build_cloud.py`(`N_ARMS=4`)，二者非同一概念 | `CloudGraph.tsx` |

---

## 9. 关键结论与风险

- 项目整体架构清晰、质量较高；四部分通过 `candidate_id` 串联，D 侧 `agent.ts::mapFinalMentor()` 与 `ragAdvisors.ts` 统一做字段缝合。
- **已完成（2026-08-22）**：
  1. ✅ 重生成 `cloud3d/cloud_data.json`（补回 `legend`/`domain_count`/`arms=4`）。
  2. ✅ 当时同步旧计数 1523/437 → 1580/500；该口径现已由 2026-09-03 的 972/1969 检索库替代，云图仍保留 715 快照。
  3. ✅ PDF 分析真实化：`pdf.ts` 已接入 `unpdf` 抽取 PDF 全文，summary/keyPoints 由内容生成，导师推荐按「文档关键词 ↔ RAG research_topics/publications」命中打分（不再按文件名）。
- **2026-09-03 RAG 质量升级已完成**：导航/模板清洗、导师身份恢复、191 个歧义平台命中的人工裁决、跨平台去重、真实总论文数映射、A–G 验证与数据质量单测均已落地。
- **剩余可择机处理**：
  - `CloudGraph.tsx` 背景装饰 `arms:6` 与 `build_cloud.py` 布局 `N_ARMS=4` 不一致（仅观感，非功能 bug）。
  - hIndex 数据缺失（全链路无，已去展示）；304 位导师无 research_topics；陈立锋、李微雪 2 位因当前导师身份依据不足未进仓。

---

## 10. 附录：各说明性文档的位置与内容（文档索引）

> 项目里散落多份说明/规划/交接文档。下表按「位置 → 内容/功能」汇总，便于新人 / agent
> 快速找到该读哪份，不必逐个翻目录。**读代码前先读根 `README.md`**，它是总入口。

| 文档 | 位置 | 内容 / 功能 |
|---|---|---|
| **README.md** | 根目录 | **总入口**。四部分速览 + 运行方式（含 A 从 `backend/src` 启动、Docker/pg 启动）+ 关键数据事实 + 端点契约。接手任何任务前先读它。 |
| **CLAUDE.md** | 根目录 | 给 Claude 编码助手的项目上下文：四部分职责表、关键数据事实、接口决策现状（已拍板）、环境（Node/Python/uv/Docker 版本）、D 协作铁律。 |
| **AGENTS.md** | 根目录 | 四名协作者（及各自 AI agent）共享的工作约定：谁管哪块、互不改对方输出格式、审批门槛。 |
| **项目全景梳理报告.md** | 根目录 | 本文件。代码级全景结论：`candidate_id` 主线、四部分细读、数据流、文档与代码不一致清单、风险。适合新人/agent 建立全局认知。 |
| **107_从比赛到长期开源项目_新版规划.md** | 根目录 | **未来规划**（非当前功能）：107 比赛版本之后如何演进为长期开源项目。属「后续方向」，不影响当前比赛版开发。 |
| **_open_research_clean.md** | 根目录 | 作者的技术思考随笔（355+ 黑客松/agent 项目如何改变思维方式）。背景性读物，非工程文档。 |
| **README.md** | `paper-claw-master/` | Paper Claw 整体项目（上游通用框架）：论文发现/解析/问答/报告生成。A 模块是其上的「导师工作流」扩展。 |
| **MENTOR_GUIDE.md** | `paper-claw-master/backend/` | 导师工作流（读模型评审）的指南/说明。 |
| **MENTOR_WORKFLOW.md** | `paper-claw-master/backend/` | A 的 FastAPI 多智能体检索后端核心文档：工作流阶段、状态、证据纪律、API。A 模块日常开发的首读文档。 |
| **README.md** | `paper-claw-master/data_scripts/` | C 模块文档：导师抓取 + RAG 库构建流程、verify_rag 检查项。 |
| **HANDOVER.md** | `cloud3d/` | B 模块交接文档：3D 星图数据 `build_cloud.py` 布局算法（N_ARMS=4）、`cloud_data.json` 接口、真实 RAG 源头。2026-08-22 已重写为与代码一致。 |
| **AGENTS.md** | `cloud3d/` | B 模块给代码助手的工作约定。 |

---

## 11. 结论

项目四部分通过 `candidate_id` 串联，已在根目录说明文档明确导航；每份说明性文档的位置与内容见上表（§10 附录：文档索引），供后续 agent / 新人接手时快速定位。本文档为代码级全景，随项目演进可能过时，建议以各模块文档 + 代码 + 磁盘数据为准。
