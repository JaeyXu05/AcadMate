# RAG 数据库、检索与“猜你喜欢”工作报告

**负责内容：** 导师 RAG 知识库构建与质量控制、导师检索链路、个性化“猜你喜欢”推荐。  
**数据基准：** 2026-09-03，以下统计均以磁盘文件 `paper-claw-master/data/ustc_mentor_rag.json` 为准。

## 一、工作概述

本部分的目标是将分散的中科大导师官方信息和论文信息沉淀为可追溯的 RAG 知识库，在此基础上提供“有证据、有边界、不以凑数为目标”的导师检索，并进一步基于用户长期兴趣、近期检索和行为偏好生成个性化导师发现推荐。

整体数据链路如下：

```text
中科大官网 / OpenAlex / Semantic Scholar / DBLP
                ↓
       清洗、身份核验、论文作者裁决、证据组装
                ↓
  ustc_mentor_rag.json（导师字段 + 可追溯 Evidence）
                ↓
离线 TF-IDF 召回 → 领域/证据/匹配审核 → 导师检索结果
                ↓
长期兴趣 + 最近检索 + 收藏/负反馈 → “猜你喜欢”个性化排序
```

## 二、RAG 数据库建设

### 1. 数据来源与组装

RAG 库以中科大教师目录和官方个人主页为身份、院系、主页、研究方向等核心事实来源；论文信息由 OpenAlex、Semantic Scholar 和 DBLP 补充。构建脚本位于 `paper-claw-master/data_scripts/build_rag.py`，统一输出候选导师（`CandidateMentor`）和证据记录（`EvidenceRecord`）。

为保证跨模块可关联，每位导师使用稳定主键 `candidate_id`，格式为 `ustc_faculty_{faculty_id}`。该 ID 同时被检索结果、导师详情、收藏/反馈及其他数据模块使用。

### 2. 当前数据成果

| 指标 | 当前结果 |
|---|---:|
| 已入库并核验的导师 | 972 位 |
| 证据记录 | 1969 条 |
| 有研究方向的导师 | 668 位 |
| 有代表论文的导师 | 259 位 |
| 有方法字段的导师 | 45 位 |
| 有招生信息的导师 | 107 位 |
| 有官网个人简介 / 邮箱的导师 | 758 / 193 位 |
| 主页、院系、单位字段覆盖 | 100% |

研究方向中，597 位来自官网个人主页，71 位在官网缺失方向时由已确认归属的论文标题进行保守回填；仍缺少方向的导师不会被伪造标签填充。论文作者匹配采取“确认后入字段”的原则：已对 191 个歧义平台命中完成人工裁决，41 个保留、150 个排除，当前不存在 `pending_review` 的论文证据进入正式候选字段。

### 3. 数据可信性设计

- 身份事实以中科大官方目录/主页证据为主，论文不能单独证明导师身份。
- 每位候选通过 `evidence_refs` 关联证据；证据含来源、定位位置、抽取事实、时间、新鲜度和支持字段，可反向追溯。
- 论文按照 DOI、平台 ID、规范化标题去重，并将“平台作品总数”与“展示用代表论文数”分开存储，避免口径混淆。
- 模糊作者命中先进行官网方向一致性校验；未确认或被裁定为错配的论文不参与研究方向、方法、论文展示与推荐打分。
- `verify_rag.py` 提供七项离线检查：schema、证据闭环、身份/角色核验、已知方向召回、覆盖率、检索质量与论文语义元数据检查。

## 三、导师检索

### 1. 检索流程

检索适配器 `FileInternalMentorRag` 直接加载 RAG JSON，在离线环境下完成初步候选召回。它会合并用户的研究方向、方法、应用领域以及领域判断扩展词，并支持导师姓名或 `candidate_id` 的精确约束。

候选文本由导师姓名、研究方向、方法、代表论文和院系组成。系统采用英文词元、中文完整短语和中文二元组构造稀疏向量，使用 TF 峰值归一与 IDF 降低高频泛词干扰；最终初排分为：

```text
召回分 = TF-IDF 余弦相似度 × 100 + 词法命中数 × 1.5
```

其中词法命中仅用于轻量级破同分，不会压过语义向量；无精确姓名/ID 约束且完全没有命中的候选不会进入结果。初步检索最多保留 80 位候选，供后续领域判断、证据审核和匹配排序使用。

### 2. 语义边界与质量门槛

为避免将宽泛上位概念误当作专业方向，检索层对生成式 AI、推荐系统、多模态生成等主题设置了可审计的语义边界：保留用户核心词、扩展同义/邻近词，同时排除“人工智能”“机器学习”等泛化父概念造成的误召回。

最终展示前还会执行候选级门槛：

- 研究方向必须来自官网核验来源；
- 匹配类型须为直接相关（`DIRECT`）或邻近相关（`ADJACENT`）；
- 每位候选必须绑定查询相关的官方/可验证证据；
- 检查论文证据和导师论文数之间是否存在矛盾；
- 若无人达到阈值，明确返回“无合格匹配”，不使用全库 Top-K 填满结果。

检索工作流在通过召回后，会完成领域研究、匹配、独立证据审核和结果编排。匹配结果保留研究主题、方法、应用、近期活跃度、学生背景适配、约束满足、招生适配、证据完整度等八维信息和证据引用，便于解释推荐来源。

### 3. 前后端接入

网站侧通过 `/api/agent/chat` 以 SSE 接收检索进度；服务端将工作流返回的嵌套 `candidate + match` 结构映射为前端导师卡片所需字段。这样前端展示与后端 RAG schema 解耦，且真实检索服务不可用时返回错误，不以 mock 导师替代真实结果。

## 四、“猜你喜欢”个性化推荐

### 1. 推荐依据与候选范围

“猜你喜欢”不是从全库按论文数简单排序，而是基于用户可解释的核心画像进行导师发现。画像由以下信号组成：

1. 长期兴趣：科研成长方向、用户填写兴趣、已收藏导师的非泛化研究方向；
2. 近期兴趣：最近 1–2 次有效检索，优先反映当前阶段的目标；
3. 行为偏好：已收藏导师优先从结果中排除以增加新发现；显式标记“不感兴趣”的导师直接过滤。

每个兴趣词都会调用与导师检索共用的 `retrieveQualifiedMentors`。因此，推荐候选同样需要通过主题相关性、语义边界、官方方向来源与证据绑定门槛；只保留 `DIRECT` 或 `ADJACENT` 匹配，避免个性化排序把无关导师重新拉入结果。

### 2. 排序机制

候选通过资格门槛后，推荐模块聚合其对多个兴趣的匹配信号，最多返回 6 位导师。推荐指数由以下因素构成：

| 因素 | 作用 |
|---|---|
| 最佳语义相关性 | 保持主题匹配是最主要依据 |
| 兴趣覆盖度 | 同时匹配多个核心兴趣的导师获得增益 |
| 近期检索信号 | 贴合用户当前搜索意图 |
| 证据充分度 | 查询相关证据及多次直接匹配提供小幅增益 |
| 资料完整度 | 研究方向数与已确认论文数仅作有限的破同分依据 |

推荐指数计算为 `65 + 语义相关性×0.25 + 各项增益`，上限为 99。结果按推荐指数、语义相关性、兴趣覆盖数量和稳定 ID 排序；界面分数会加入最小递减间隔，以减少多个导师显示为完全相同分数的情况，但不会改变排名。

这里的分数被明确标记为 `personalized_recommendation`：它表示“对该用户画像的相对推荐指数”，**不等同于**检索页面针对单次查询展示的绝对匹配分。接口会同步返回实际命中的兴趣项和评分构成，页面展示推荐依据，保障可解释性。

### 3. 用户反馈闭环

系统提供导师反馈表 `advisor_feedback`。用户对某位导师点击“不感兴趣”后，记录按“用户 + 导师”唯一键更新，并在后续推荐中稳定排除；撤销反馈后可重新进入候选池。收藏与负反馈承担不同语义：收藏用于保存感兴趣对象并帮助挖掘相近方向，负反馈用于直接过滤，不把一次负反馈扩展为整类研究方向的降权，避免过度推断用户偏好。

## 五、当前完成情况与后续优化方向

目前已形成从数据构建、证据核验、离线可复现检索到个性化推荐的完整闭环，并为 RAG 质量和推荐排序提供了自动化检查/单元测试入口。当前的主要数据改进方向是继续补齐 304 位导师缺失的官网研究方向、提高招生与论文覆盖率；推荐侧可在积累更多真实反馈后，再评估是否引入更细粒度的正向反馈权重与多样性约束。

本阶段坚持的原则是：先保证身份、主题和证据真实可追溯，再追求召回规模与推荐丰富度。
